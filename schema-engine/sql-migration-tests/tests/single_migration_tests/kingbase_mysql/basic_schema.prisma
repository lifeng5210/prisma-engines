// tags=KingbaseMysql
datasource db {
  provider = "kingbase-mysql"
}

model Account {
  id    Int    @id
  email String @unique @db.VarChar(64)
  posts Post[]
}

model Post {
  id        Int      @id
  accountId Int
  status    Status   @default(DRAFT)
  createdAt DateTime @default(now()) @db.DateTime(3)
  account   Account  @relation(fields: [accountId], references: [id], onDelete: Cascade)

  @@index([accountId], map: "Post_accountId_idx")
}

enum Status {
  DRAFT
  PUBLISHED
}

// Expected Migration:
// -- CreateTable
// CREATE TABLE `Account` (
//     `id` INTEGER NOT NULL,
//     `email` VARCHAR(64) NOT NULL,
// 
//     UNIQUE INDEX `Account_email_key`(`email`),
//     PRIMARY KEY (`id`)
// );
// 
// -- CreateTable
// CREATE TABLE `Post` (
//     `id` INTEGER NOT NULL,
//     `accountId` INTEGER NOT NULL,
//     `status` ENUM('DRAFT', 'PUBLISHED') NOT NULL DEFAULT 'DRAFT',
//     `createdAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
// 
//     INDEX `Post_accountId_idx`(`accountId`),
//     PRIMARY KEY (`id`)
// );
// 
// -- AddForeignKey
// ALTER TABLE `Post` ADD CONSTRAINT `Post_accountId_fkey` FOREIGN KEY (`accountId`) REFERENCES `Account`(`id`) ON DELETE CASCADE ON UPDATE CASCADE;
